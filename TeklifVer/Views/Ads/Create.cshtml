@using Entities;
@using TeklifVer.UI.Models.Car;
@model AdvertisingCreateModel
@{
    ViewData["Title"] = "Create";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


@*
<form method="post" asp-action="create" asp-controller="CarModel" enctype="multipart/form-data">

    <div class="form-group">
        <label asp-for="@Model" class="form-label">Model Seçiniz:</label>
        <select asp-for="@Model.Model" class="form-select">
            <option selected>Marka Seçiniz...</option>

            @foreach (var item in Model.BrandsList)
            {
                    <option value="@item.Definition">@item.Definition</option>
            }
        </select>
    </div>

    <div class="form-group mt-3">
        <label asp-for="@Model.Image" class="form-label">Model:</label>
        <input class="form-control" type="text" asp-for="@Model.Definition" />
    </div>


    <div class="form-group mt-3">
        <label asp-for="@Model.Image" class="form-label">Resim:</label>
        <input class="form-file" type="file" asp-for="@Model.Image" />
     </div>
    <button type="submit" class="btn btn-primary mt-3">Oluştur</button>


</form>
*@

