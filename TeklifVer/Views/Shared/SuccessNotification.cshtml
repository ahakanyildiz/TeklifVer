@model (bool,string)



<script>
    var message = '@Model.Item2';

    @if (Model.Item1)
    {
        @:alertify.success(message);
    }
    else
    {
        @:alertify.error(message);
    }
</script>